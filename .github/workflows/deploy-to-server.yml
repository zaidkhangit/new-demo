name: deploy to server
on:
  workflow_call:
jobs:
  deploying:
    runs-on: ubuntu-latest
    env:
      DOCKERHUB_USER: ${{ vars.DOCKERUSER }}
      DOCKER_TAG: ${{ github.sha }} 
    steps:
      - name: checkout code
        uses: actions/checkout@v4
        
      - name: ssh to pro server & create app
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ vars.SSH_USERNAME }}
          key: ${{ secrets.KEY}}
          scripts: |
            echo " start deploy"
            sudo apt-get update && sudo apt-get install docker.io docker-compose-v2 -y
            sudo usermod -aG docker $USER
            mkdir -p devops
            
      - name: Copy the docker-compose file
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ vars.DOCKERUSER}}
          key: ${{ secrets.KEY }}
          secrets: inherit
          source: docker-compose.yml
          target: ~/devops  
          
      - name: SH to prod derver & run app
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ vars.SSH_USERNAME }}
          key: ${{ secrets.KEY }}    
          script:  |
            echo " start deploy"
            export DOCKERUSER=${{ vars.DOCKERUSER }}
            DOCKER_TAG=${{ github.sha}}
            echo ${{ github.sha }}
            export DOCKER_PASSWORD=${{ secrets.DOCKER_PASSWORD }}
            echo ${{ secrets.DOCKER_PASSWORD }} | docker login --username ${{ vars.DOCKERUSER }} --password-stdin
            cd ~/devops && docker compose down && docker compose up -d --build --force-recreate --pull always
